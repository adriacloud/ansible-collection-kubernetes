# helm_deploy

The role allows to fetch and deploy helm charts. You can supply a local
ref or remote.
Alternatively, a git repository could be supplied, which will be cloned, and
helm chart will be installed from it, as if it's a local one.
